'use strict';

/* Numbers

TODO: create a few variables(numbers):
- you need to add comments to show where the variable been
declared, initialized, read , reassigned,...
*/
//declare a variable length and initilize it to the value 16
let length = 16;
// read the variable length and value is 16
console.log(length);
// reassigned the value of variable length
length = 7.5;
// read the variable length and value is 7.5
console.log(length);